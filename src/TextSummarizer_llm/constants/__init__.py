#The path that will not change throughout the developmentment process like config.yaml or param.yaml,these files are kept here.

from pathlib import Path

CONFIG_FILE_PATH=Path("config/config.yaml")
PARAMS_FILE_PATH=Path("params.yaml")